import { makeStyles } from "@mui/styles";

export const useButtonStyles = makeStyles(() => ({
  roundButton: {
    borderRadius: "100%",
    padding: 10,
  },

  ovalButton: {
    borderRadius: 30,
  },
}));
